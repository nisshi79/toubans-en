$('#members_list').addInputArea(/*{
    after_add : function () {
        var addedNum = $('#roles_list li').length;
        var addedId = addedNum-1;
        $('members_list_' + addedId).attr('placeholder', '例）田中さん／'+addedNum+'班');
    }


}*/);
$('#roles_list').addInputArea();
$('#spans_list_0').daterangepicker({
    opens: 'left',
    autoUpdateInput: false,
    locale:{
        format: "YYYY/MM/DD"
    }
}, function(start, end, label) {
    console.log("A new date selection was made: " + start.format('YYYY-MM-DD') + ' to ' + end.format('YYYY-MM-DD'));
});

$('#spans_list').addInputArea({

    clone_event: false,
    after_add: function () {
        var countA = 1;
    var countA = $('#spans_list li').length;
    var countBuf = countA-1;
    console.log(countBuf);
    $('#spans_list_'+countBuf).daterangepicker({
        autoUpdateInput : true,

        opens: 'left',
        locale:{
            format: "YYYY/MM/DD"
        }
    }, function(start, end, label) {
        console.log("A new date selection was made: " + start.format('YYYY-MM-DD') + ' to ' + end.format('YYYY-MM-DD'));
    })
        .val('');

}
});